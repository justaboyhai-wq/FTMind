# FMind 2026-08-19 工作改动汇总

> 本文记录当天在本地源码、容器化部署、宝安政策 RSS 接入及知识库迁移方面完成或纳入当前工作区的改动。部署过程中生成的 Docker 镜像归档不纳入 Git：它们是可再生的大型二进制文件，应通过构建或镜像仓库获取。

## 1. 品牌与界面

- 项目品牌统一调整为 **FMind**，包含前端标题、浏览器图标、登录页、菜单和集成说明。
- 下载并使用本地顺丰风格品牌图标资产，替换外链 Logo；浏览器标签页图标与站点图标同步更新。
- 全局主题调整为蓝色系，覆盖按钮、状态、边框和登录页视觉。
- 登录页重构为 FMind 品牌认证页，移除旧知识图谱动画与旧文案。
- 新增/调整外部记忆、组织、租户成员、技能集成、模型配置等管理界面与中文文案。

## 2. 认证、组织与权限

- 登录响应、用户、空间、成员资格与权限快照的前端状态同步逻辑增强。
- 支持组织、团队、团队成员和团队智能体的管理模型及前端配置入口。
- 外部 Agent 绑定增加团队、用户 API Key、能力范围、资产范围和一次性接入提示词等约束。
- 管理员/所有者菜单与外部记忆权限路径纳入路由及命令面板。

## 3. 对话反馈闭环

- 新增问答反馈前端组件及 API：普通对话、Agent 流式对话、嵌入式对话均可提交反馈。
- 新增反馈数据模型、迁移、仓储、服务、Handler 与路由。
- 支持反馈类别、问题/回答/引用快照、补充评论、重新打开、管理员状态流转、根因、处置与公开回复。
- 新增“我的反馈”和管理员“反馈管理”页面及 CSV 导出能力。

## 4. 外部记忆与记忆 Wiki

- 完善外部记忆绑定、审查、发布、撤销与访问范围校验。
- 增强记忆 Wiki 发布、接入密钥轮换、内部记忆与 MCP cognition 接线。
- 补充外部记忆 API、组织 API、团队 API、测试及权限校验。

## 5. 文档解析与安全

- 修复/更新 DocReader protobuf 生成物与 Docker 构建配置。
- EPUB、PPTX 解析增加归档安全限制，降低 ZIP 压缩炸弹造成的内存耗尽风险。
- 增加归档安全测试及解析回归测试。

## 6. 宝安政策采集、RSS 与知识库同步

- 宝安政策采集器支持官网索引解析、明细解析、关系、附件、字典、规范化 Markdown 和稳定文件名输出。
- 政策文件命名统一为“官方标题（post_编号）.md”，保留稳定业务 ID 与快照追溯信息。
- RSS 输出稳定 GUID、正文、来源 URL、附件、官网分类标签及标签审计接口。
- FMind RSS 连接器支持读取 `<category>` 标签、稳定政策文件名、标签变更增量同步和正文下载回退。
- 新增存量知识库迁移工具：文件名迁移、官网标签补建、RSS 接管游标、回滚清单、Wiki 引用更新与幂等保护。
- 迁移和同步逻辑确保分类变化尽可能原地更新既有知识，保留知识 ID、分块、向量、Wiki 与手工标签。
- 增加政策采集器本地 Docker Compose、README、迁移测试和 RSS/服务端回归测试。

## 7. 部署与运行

- Docker Compose、应用镜像、DocReader 镜像、前端镜像和内存服务配置更新。
- 新增云端测试环境部署手册，FMind 使用 18310，宝安政策 RSS 使用 18320。
- 完善开发环境端口、服务依赖、镜像构建和本地技能发布脚本。

## 8. 验证记录

- 宝安政策采集器：`go test ./...`、`go vet ./...` 通过。
- RSS 连接器及相关应用服务测试通过；`git diff --check` 通过。
- 前端曾执行 `npm run type-check` 通过。
- DocReader、SQLite 相关根仓库全量测试在本地受 CGO/环境依赖影响，未将该环境问题当作功能通过结论。

## 9. 当前已知待处理项

- 云端登录接口本身可快速返回成功；但应用内浏览器登录后前端仍可能停留在“登录中…”，需继续定位登录后的状态刷新与路由链路。
- 反馈模块已具备前后端实现，但 `feedback.*` 多语言键需要在中英文语言包中完整补齐，以确保“反馈管理”等菜单与页面文案可见。
- Docker 镜像归档、部署压缩包等大型二进制产物未提交到 Git。
